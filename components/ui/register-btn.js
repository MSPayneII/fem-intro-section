import React from "react";
import classes from "./buttons.module.css";

const RegisterBtn = () => {
  return <button className={classes.registerBtn}>Register</button>;
};

export default RegisterBtn;
